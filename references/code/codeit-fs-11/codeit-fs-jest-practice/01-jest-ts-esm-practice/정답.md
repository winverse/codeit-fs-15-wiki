# 01-jest-ts-esm 정답

이 문서는 1장 실습의 정답 기준입니다.

## 최종적으로 맞아야 할 파일

- `package.json`
- `tsconfig.json`
- `jest.config.mjs`
- `src/shipping-fee.spec.ts`

## 정답

파일: `package.json`

```json
{
  "name": "@express-test/01-jest-ts-esm",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "imports": {
    "#src/*.js": {
      "types": "./src/*.ts",
      "default": "./dist/src/*.js"
    },
    "#test/*.js": {
      "types": "./test/*.ts",
      "default": "./dist/test/*.js"
    }
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@jest/globals": "^30.3.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.6.0",
    "cross-env": "^10.1.0",
    "eslint": "^10.2.1",
    "jest": "^30.3.0",
    "prettier": "^3.8.3",
    "ts-jest": "^29.4.9",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.0"
  },
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --config ./jest.config.mjs --runInBand",
    "test:cov": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --config ./jest.config.mjs --coverage --runInBand",
    "test:watch": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --config ./jest.config.mjs --watch",
    "typecheck": "tsc -p ./tsconfig.json --noEmit",
    "lint": "eslint .",
    "format": "prettier . --write",
    "format:check": "prettier . --check"
  }
}
```

파일: `tsconfig.json`

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "isolatedModules": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "types": ["node", "jest"],
    "rootDir": ".",
    "outDir": "./dist"
  },
  "include": ["src/**/*.ts"]
}
```

파일: `jest.config.mjs`

```js
export default {
  testEnvironment: 'node',
  testMatch: ['<rootDir>/src/**/*.spec.ts'],
  extensionsToTreatAsEsm: ['.ts'],
  moduleFileExtensions: ['ts', 'js', 'json'],
  transform: {
    '^.+\\.ts$': [
      'ts-jest',
      {
        useESM: true,
        tsconfig: '<rootDir>/tsconfig.json',
      },
    ],
  },
  moduleNameMapper: {
    '^(\\.{1,2}/.*)\\.js$': '$1',
    '^#src/(.*)\\.js$': '<rootDir>/src/$1',
    '^#test/(.*)\\.js$': '<rootDir>/test/$1',
  },
};
```

파일: `src/shipping-fee.spec.ts`

```ts
import { shippingFee } from '#src/shipping-fee.js';

describe('shippingFee', () => {
  test('주문 금액이 50,000원 미만이면 배송비 3,000원을 반환한다', () => {
    expect(shippingFee(49_000)).toBe(3_000);
  });

  test('주문 금액이 50,000원 이상이면 배송비 0원을 반환한다', () => {
    expect(shippingFee(50_000)).toBe(0);
  });
});
```
